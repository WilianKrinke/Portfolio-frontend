

const crudRoute = (app) => {
    app.route("users/:id?")
        .get(() => {

        })
        .post(() =>{

        })
        .put(() => {

        })
        .delete(() => {

        })
}

module.exports = crudRoute;
